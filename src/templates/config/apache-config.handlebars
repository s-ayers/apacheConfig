<VirtualHost {{virtualHost}}:80>
  {{#if serverName}}ServerName {{serverName}}{{/if}}
  {{#if documentRoot}}DocumentRoot {{documentRoot}}{{/if}}
  {{#if ServerAlias}}ServerAlias {{serverAliases}}{{/if}}

  DirectoryIndex index.html

  <Directory "{{documentRoot}}">
      Options Indexes FollowSymLinks
      allow from all
      Options -Indexes
      Options +FollowSymLinks
  </Directory>

</VirtualHost>

{{#if ssl}}
<VirtualHost {{virtualHost}}:443>
     SSLEngine On
     SSLCertificateFile /etc/apache2/ssl/binaryalchemist.crt
     SSLCertificateKeyFile /etc/apache2/ssl/binaryalchemist.key

  {{#if serverName}}ServerName {{serverName}}{{/if}}
  {{#if ServerAlias}}ServerAlias {{serverAliases}}{{/if}}
  {{#if documentRoot}}DocumentRoot {{documentRoot}}
  <Directory "{{documentRoot}}">
      Options Indexes FollowSymLinks
      allow from all
      Options -Indexes
      Options +FollowSymLinks
  </Directory>
{{/if}}
</VirtualHost>
{{/if}}
